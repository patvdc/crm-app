LogBack + log what ?

? zip-table met  (type-ahead service)

? interface status getStatus with all values of enum (ivo)

CREATE SCHEMA `crm` DEFAULT CHARACTER SET utf8 ;

script to create scheme
schema.sql not needed as tables will be created by jpa ?

USE crm;
SET foreign_key_checks = 0;
DROP TABLE IF EXISTS contacts;
SET foreign_key_checks = 1;
CREATE TABLE Contacts ...

? facebook-picture on contact

//ex values NEW, CONTACTED,MEETING,WON,LOST

